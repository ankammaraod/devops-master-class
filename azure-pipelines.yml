# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

stages:
  - stage: FistStage
    jobs:
      - job: FirstJob
        steps:
          - script: echo 1st_step inside 1st_job inside 1st_stage
      - job: SecondJob
        steps:
          - script: |
              echo ankamma
              echo hai
          - script: |
              echo Roshan
              echo hai
      - job: ThirdJob
        steps:
          - script: |
              echo sampu
              echo bye
          - script: |
              echo nilam
              echo ja rahey
